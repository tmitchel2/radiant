namespace Radiant.Components.Primitives;

/// <summary>How floating content lines up along the side of its anchor it sits on.</summary>
public enum SideAlign
{
    /// <summary>With the anchor's start (left, or top).</summary>
    Start,

    /// <summary>Centred on the anchor.</summary>
    Center,

    /// <summary>With the anchor's end (right, or bottom).</summary>
    End,
}
