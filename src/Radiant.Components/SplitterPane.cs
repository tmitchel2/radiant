namespace Radiant.Components;

/// <summary>Which pane of a <see cref="Splitter"/> has the set size (the other takes the rest).</summary>
public enum SplitterPane
{
    /// <summary>The left or top pane.</summary>
    First,

    /// <summary>The right or bottom pane.</summary>
    Second,
}
