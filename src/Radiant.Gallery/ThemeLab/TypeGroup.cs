namespace Radiant.Gallery.ThemeLab;

/// <summary>The type roles the editor sets a font and weight for together.</summary>
internal enum TypeGroup
{
    Display,
    Headline,
    Title,
    Body,
    Label,
    Code,
}
