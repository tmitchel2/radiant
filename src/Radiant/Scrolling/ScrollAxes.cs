namespace Radiant.Scrolling;

/// <summary>Which axes a scroller responds on.</summary>
public enum ScrollAxes
{
    /// <summary>Vertical only (the default).</summary>
    Vertical,

    /// <summary>Horizontal only.</summary>
    Horizontal,

    /// <summary>Both axes (free 2D scrolling).</summary>
    Both,
}
